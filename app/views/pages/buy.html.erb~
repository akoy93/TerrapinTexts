<% provide(:title, "Buy") %>

<% if current_user %>
  <div class="well">
    <h2 class="center">Search for a Textbook by ISBN:</h2>
    <br />
    <%= form_tag("/search", method: "get", class: "form-search center", id: "buy_search") do %>
      <input id="isbn" name="isbn" type="text" placeholder="Enter a 10 or 13 digit ISBN number">
      <%= submit_tag("Search", class: "btn") %>
    <% end %>
  </div>
  <div id="search_results"><%= render 'search_results' %></div>
<% else %>
  <div class="alert alert-error">
    You must sign in with Facebook in order to access this page!
  </div>
<% end %>

