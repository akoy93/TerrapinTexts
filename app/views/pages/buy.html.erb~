<% provide(:title, "Buy") %>

<% if current_user %>
  <div class="well">
    <h3 class="center">Search for a Textbook by ISBN:</h3>
    <br />
    <%= form_tag("/search", method: "get", class: "form-search center") do %>
      <input id="isbn" name="isbn" type="text" placeholder="Enter a 13-digit ISBN number">
      <%= submit_tag("Search", class: "btn") %>
    <% end %>
  </div>
  <%= render 'layouts/search_results' %>
<% else %>
  <div class="alert alert-error">
    You must sign in with Facebook in order to access this page!
  </div>
<% end %>

