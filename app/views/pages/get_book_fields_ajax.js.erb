// render dynamics listing form
$("#dynamic_listing_form").html("<%= escape_javascript(render("listing_form")) %>");
$("#textbook_listing_isbn").focusout(function() {
  $.get($(this).attr("action"), $(this).serialize(), null, "script");
  return false;
});
