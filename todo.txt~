features to implement:
  -> display mutual friends
    ~ on sign in, create a set of friend uids, and method to query set for contains

  -> 10 listings max per seller
  -> optimize live search
  -> autocomplete
    ~ submit on select
  -> sort listings by most recent as default

  -> db migration
    ~ class column, email column, first name column
    ~ load email and first name into listing row when created
  -> add contact information

  -> insert class into column as rake task
